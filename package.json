{
  "name": "webshop-template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write . && npm run lint:fix --prefix functions",
    "write:indexes": "firebase firestore:indexes > firestore.indexes.json"
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "prettier-plugin-svelte": "3.2.6"
  }
}
